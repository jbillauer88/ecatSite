json.extract! sample, :id, :name, :audioFile, :latitude, :longitude, :locPhoto, :temperature, :altitude, :gain, :recDevice, :addNotes, :created_at, :updated_at
json.url sample_url(sample, format: :json)
